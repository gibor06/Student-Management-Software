<Window x:Class="Student_Management_System.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Student_Management_System.ViewModels"
        mc:Ignorable="d"
        Title="Hệ thống quản lý sinh viên"
        Width="1000"
        Height="660"
        MaxWidth="{x:Static SystemParameters.PrimaryScreenWidth}"
        MaxHeight="{x:Static SystemParameters.PrimaryScreenHeight}"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="#EEF3FB"
        Opacity="0"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.InputBindings>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveJsonCommand}"/>
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding LoadJsonCommand}"/>
        <KeyBinding Key="E" Modifiers="Control" Command="{Binding ExportCsvCommand}"/>
        <KeyBinding Key="Z" Modifiers="Control" Command="{Binding UndoCommand}"/>
        <KeyBinding Key="Y" Modifiers="Control" Command="{Binding RedoCommand}"/>
    </Window.InputBindings>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.24"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <vm:InverseBooleanConverter x:Key="InverseBoolConverter"/>

        <SolidColorBrush x:Key="TextBrush" Color="#13213C"/>
        <SolidColorBrush x:Key="SubtleBrush" Color="#61718C"/>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D8E2F1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="14" ShadowDepth="0" Opacity="0.08" Color="#274363"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="{StaticResource SubtleBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InputTextStyle" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderBrush" Value="#C8D6EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FCFDFF"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#1F6FEB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#C8D6EA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FCFDFF"/>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="#1F6FEB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="106"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1F6FEB"/>
            <Setter Property="BorderBrush" Value="#1658BB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.82"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OutlineBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="#C5D2E5"/>
        </Style>

        <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#C83A51"/>
            <Setter Property="BorderBrush" Value="#A82A40"/>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#2D7EBC"/>
            <Setter Property="BorderBrush" Value="#1D679F"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="AlternatingRowBackground" Value="#F7FAFF"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="ColumnHeaderHeight" Value="44"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#EDF3FD"/>
            <Setter Property="Foreground" Value="#223555"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="BorderBrush" Value="#D9E4F4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Foreground" Value="#13233E"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="BorderBrush" Value="#E6EDF9"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EFF5FF"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#DCEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="20,20,20,12" Padding="24" CornerRadius="20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#173A7A" Offset="0"/>
                    <GradientStop Color="#1E66DB" Offset="0.62"/>
                    <GradientStop Color="#2CB1D8" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <DockPanel>
                <StackPanel>
                    <TextBlock Text="Hệ thống quản lý sinh viên" FontSize="34" FontWeight="SemiBold" Foreground="White"/>
                </StackPanel>
                
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Margin="20,0,20,10" Style="{StaticResource CardStyle}" Padding="10">
            <TabControl>
                <TabItem Header="Quản lý sinh viên">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled"
                                  CanContentScroll="False">
                        <Grid Margin="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <UniformGrid Grid.Row="0" Columns="3" Margin="0,0,0,12">
                            <Border Margin="0,0,10,0" Style="{StaticResource CardStyle}" BorderBrush="#CEE0F8">
                                <StackPanel>
                                    <TextBlock Text="Tổng số sinh viên" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding TongSoSinhVien}" FontSize="32" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Margin="0,0,10,0" Style="{StaticResource CardStyle}" BorderBrush="#CDEEEA">
                                <StackPanel>
                                    <TextBlock Text="Sinh viên nam" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding SoNam}" FontSize="32" FontWeight="SemiBold" Foreground="#0F7268" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}" BorderBrush="#F2D5DB">
                                <StackPanel>
                                    <TextBlock Text="Sinh viên nữ" Style="{StaticResource LabelStyle}"/>
                                    <TextBlock Text="{Binding SoNu}" FontSize="32" FontWeight="SemiBold" Foreground="#B4384F" Margin="0,5,0,0"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.85*"/>
                                <ColumnDefinition Width="1.15*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Margin="0,0,10,0" Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="160"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Thông tin sinh viên" Style="{StaticResource SectionStyle}" Margin="0,0,0,10"/>

                                    <TextBlock Grid.Row="1" Text="Họ và tên" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource InputTextStyle}" Text="{Binding InputHoTen, UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock Grid.Row="2" Text="Tuổi" Style="{StaticResource LabelStyle}"/>
                                    <TextBox Grid.Row="2" Grid.Column="1" Style="{StaticResource InputTextStyle}" Text="{Binding InputTuoi, UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock Grid.Row="3" Text="Giới tính" Style="{StaticResource LabelStyle}"/>
                                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,8,0,6">
                                        <RadioButton Content="Nam" Margin="0,0,18,0" GroupName="GT" IsChecked="{Binding InputGioiTinh}"/>
                                        <RadioButton Content="Nữ" GroupName="GT" IsChecked="{Binding InputGioiTinh, Converter={StaticResource InverseBoolConverter}}"/>
                                    </StackPanel>

                                    <TextBlock Grid.Row="4" Text="Thành phố" Style="{StaticResource LabelStyle}"/>
                                    <ComboBox Grid.Row="4" Grid.Column="1" Style="{StaticResource InputComboStyle}" ItemsSource="{Binding ThanhPhos}" SelectedItem="{Binding InputThanhPho}"/>
                                </Grid>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Thao tác nhanh" Style="{StaticResource SectionStyle}" Margin="0,0,0,10"/>

                                    <WrapPanel Grid.Row="1" Margin="0,0,0,8">
                                        <Button Content="Thêm" Command="{Binding AddCommand}"/>
                                        <Button Content="Sửa" Command="{Binding EditCommand}"/>
                                        <Button Content="Xóa" Style="{StaticResource DangerBtn}" Command="{Binding DeleteCommand}"/>
                                        <Button Content="Undo" Style="{StaticResource SecondaryBtn}" Command="{Binding UndoCommand}"/>
                                        <Button Content="Redo" Style="{StaticResource SecondaryBtn}" Command="{Binding RedoCommand}"/>
                                        <Button Content="Xuất CSV" Style="{StaticResource SecondaryBtn}" Command="{Binding ExportCsvCommand}"/>
                                        <Button Content="Làm mới" Style="{StaticResource OutlineBtn}" Command="{Binding ClearInputCommand}"/>
                                    </WrapPanel>

                                    <Border Grid.Row="2" Background="#F6FAFF" BorderBrush="#DCE6F4" BorderThickness="1" CornerRadius="11" Padding="12">
                                        <StackPanel>
                                            <TextBlock Text="Thông tin đang chọn" Style="{StaticResource LabelStyle}"/>
                                            <TextBlock Margin="0,8,0,0">
                                                <Run Text="Sinh viên: "/>
                                                <Run Text="{Binding SelectedStudent.HoTen, Mode=OneWay, TargetNullValue=Chưa chọn}" FontWeight="SemiBold"/>
                                            </TextBlock>
                                            <TextBlock Margin="0,4,0,0">
                                                <Run Text="Thành phố: "/>
                                                <Run Text="{Binding SelectedStudent.ThanhPho, Mode=OneWay, TargetNullValue=---}" FontWeight="SemiBold"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </Grid>

                        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="Từ khóa" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                                        <TextBox Width="210" Style="{StaticResource InputTextStyle}" Margin="0,0,12,0" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                                        <TextBlock Text="Thành phố" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                                        <ComboBox Width="160" Style="{StaticResource InputComboStyle}" Margin="0,0,12,0" ItemsSource="{Binding ThanhPhoBoLoc}" SelectedItem="{Binding FilterThanhPho}"/>
                                        <TextBlock Text="Giới tính" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                                        <ComboBox Width="115" Style="{StaticResource InputComboStyle}" Margin="0,0,12,0" ItemsSource="{Binding GioiTinhBoLoc}" SelectedItem="{Binding FilterGioiTinh}"/>
                                        <TextBlock Text="Sắp xếp" Style="{StaticResource LabelStyle}" Margin="0,0,8,0"/>
                                        <ComboBox Width="165" Style="{StaticResource InputComboStyle}" ItemsSource="{Binding SapXepTuyChon}" SelectedItem="{Binding SortOption}"/>
                                    </WrapPanel>

                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="{StaticResource SubtleBrush}">
                                        <Run Text="Tổng dữ liệu: "/>
                                        <Run Text="{Binding TongSoSinhVien, Mode=OneWay}"/>
                                    </TextBlock>
                                </Grid>

                                <Border Grid.Row="1" Height="300" BorderBrush="#D2DEEE" BorderThickness="1" CornerRadius="10" Background="White">
                                    <DataGrid ItemsSource="{Binding StudentsView}"
                                              SelectedItem="{Binding SelectedStudent}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Họ và tên" Width="2.2*" Binding="{Binding HoTen}"/>
                                            <DataGridTextColumn Header="Tuổi" Width="*" Binding="{Binding Tuoi}"/>
                                            <DataGridTextColumn Header="Giới tính" Width="*" Binding="{Binding GioiTinhText}"/>
                                            <DataGridTextColumn Header="Thành phố" Width="1.5*" Binding="{Binding ThanhPho}"/>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </Grid>
                        </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Lưu/Tải dữ liệu">
                    <Grid Margin="14">
                        

                        <Border Margin="0,0,12,0" Style="{StaticResource CardStyle}">
                            <StackPanel>
                                <TextBlock Text="Lưu trữ dữ liệu" Style="{StaticResource SectionStyle}"/>
                                <TextBlock Margin="0,10,0,16" TextWrapping="Wrap" Foreground="{StaticResource SubtleBrush}" Text="Hỗ trợ lưu/tải JSON và xuất CSV để làm báo cáo hoặc nhập Excel."/>
                                <Button Content="Lưu file JSON" Command="{Binding SaveJsonCommand}" Margin="0,0,0,10"/>
                                <Button Content="Tải file JSON" Style="{StaticResource OutlineBtn}" Command="{Binding LoadJsonCommand}" Margin="0,0,0,10"/>
                                <Button Content="Xuất file CSV" Style="{StaticResource SecondaryBtn}" Command="{Binding ExportCsvCommand}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,20" Padding="12,8" Style="{StaticResource CardStyle}">
            <DockPanel>
                <TextBlock Foreground="{StaticResource SubtleBrush}" Text="{Binding ThongBaoTrangThai}" VerticalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Right" Foreground="#6A7891" FontSize="12" VerticalAlignment="Center" Text="Ctrl+S | Ctrl+O | Ctrl+E | Ctrl+Z | Ctrl+Y"/>
            </DockPanel>
        </Border>
    </Grid>
</Window>
